import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tgallery.app',
  appName: 'com.tgallery.app',
  webDir: 'dist/myapp/browser'
};

export default config;
